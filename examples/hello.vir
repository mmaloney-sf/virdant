pub mod Top {
    incoming clk : Clock;
    incoming reset : Word[1];
    outgoing out : Word[1];

    out := reset;

    mod foo of Foo;
}

mod Foo {
    incoming in : Word[1];
    incoming out : Word[1];

    node tmp : Word[1];
    tmp := in;
    out := tmp;
}
